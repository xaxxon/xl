set(FOO_VERSION @XL_VERSION@)

get_filename_component(
        EGGS_VARIANT_CMAKE_DIR "${CMAKE_CURRENT_LIST_DIR}" ABSOLUTE)

include("${EGGS_VARIANT_CMAKE_DIR}/xlTargets.cmake")


#include(xlTargets.cmake)

@PACKAGE_INIT@

set_and_check(FOO_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

find_package(fmt)
if (fmt_FOUND)
    add_definitions(-DXL_FMT_FOUND)
endif()